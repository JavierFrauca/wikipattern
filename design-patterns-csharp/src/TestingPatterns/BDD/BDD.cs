using System;

// BDD Pattern
public class Calculator
{
    public int Add(int a, int b) => a + b;
}

// Ejemplo de uso (Given, When, Then)
// var calc = new Calculator(); // Given
// var result = calc.Add(2, 3); // When
// if (result == 5) Console.WriteLine("Then: resultado correcto"); // Then
